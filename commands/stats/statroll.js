/*const { Command } = require("discord.js-commando");
const rn = require("random-number");

module.exports = class StatRollCommand extends Command {
    constructor(client) {
        super(client, {
            name: 'statroll',
            group: 'stats',
            memberName: 'statroll',
            description: 'Allows players to quickly perform rolls for stats',
            examples: ['statroll']
        });
    }

    run(message) {
        var statgen = rn.generator({
            min: 1,
            max: 6,
            integer: true
        });
        for (i = 0; i < 4; i++) {
            var 
        }
    }
}*/